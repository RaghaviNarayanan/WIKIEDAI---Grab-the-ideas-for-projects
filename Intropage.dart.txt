



//////////////////////////////   intro ///////////////////////////////////////

import 'dart:ui';

import 'package:flutter/material.dart';

import 'main.dart';

class IntroPage extends StatefulWidget {
  const IntroPage({super.key});

  @override
  State<IntroPage> createState() => _IntroPageState();
}

class _IntroPageState extends State<IntroPage> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Welcome to WikiEdai",style: TextStyle(color: Colors.white,fontStyle: FontStyle.italic),),
        backgroundColor: Color(0xFF8C0380),
        centerTitle: true,
      ),
      body: Stack(
        children: [
          Container(
            decoration: BoxDecoration(
              image: DecorationImage(
                image: AssetImage(
                    "images/App_blue.jpg"),
                fit: BoxFit.cover,
              ),
            ),
          ),
          Center(
            child: Column(
              mainAxisAlignment: MainAxisAlignment.center,
              crossAxisAlignment: CrossAxisAlignment.center,
              children: [
                Text(
                  "Welcome to the app!",
                  style: TextStyle(
                    fontSize: 20,
                    fontStyle: FontStyle.italic,
                    color: Color(0xfff4f4f2),
                  ),
                ),
                SingleChildScrollView(
                  child: Padding(
                    padding: EdgeInsets.all(20),
                    child: Text(
                      "This is the only platform in which you can find your desired project at one place at one go!! \n Get into world of enginners. Have fun!!",
                      style: TextStyle(color: Colors.white ,
                      fontSize: 20),

                    ),
                  ),
                ),
                SizedBox(
                  height: 30,
                ),
                SizedBox(
                  height: 50,
                  width: 250, // Adjusted height
                  child: ElevatedButton.icon(
                    onPressed: () {
                      Navigator.pushReplacement(context, MaterialPageRoute(builder: (context)=>DomainListPage(),));
                    },
                    label: Text("Let's get started!"),
                    icon: Icon(Icons.search), // Add button text
                    style: ElevatedButton.styleFrom(
                      foregroundColor: Color(0xffffffff), backgroundColor: Color(0x8C0380FF),
                      elevation: 15,
                      shadowColor: Color(0xffa87ef9),
                      shape: RoundedRectangleBorder(
                        borderRadius:
                        BorderRadius.circular(10), // Customize button shape
                      ),
                      side: BorderSide(
                        color: Colors.white,
                        width: 2,
                      ),
                      textStyle: TextStyle(
                        fontSize: 20,
                        fontStyle: FontStyle.italic,
                      ),
                    ),
                  ),
                ),
                // Add more widgets here if desired
              ],
            ),
          ),
        ],
      ),
    );
  }
}



